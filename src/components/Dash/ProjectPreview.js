import React from 'react'

const ProjectPreview = ({ PostPreview }) => {
    return(
      <div className='PostPreview'>
        <p>{PostPreview}</p>
      </div>
    );
  }

export default ProjectPreview
